<%= render 'shared/content_title', title: "Categories" %>

<ul>
<% @categories.each do |category| %>
  <% number_of_posts =  " <span class='quiet'>(#{category.posts.size} posts)</span>".html_safe%>
  <li><%= link_to(category.name, category_path(category)) + number_of_posts %></li>
<% end %>
</ul>